---
name: Continuous Build
on:
  pull_request:
    branches:
      - main
  workflow_call:

jobs:
  Build:
    name: Different Build Confgurations
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust
        if: runner.os == 'Linux'
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
      - name: Cargo Build
        run: cargo build
      - name: Cargo Build Dev
        run: cargo build --features dev
      - name: Cargo Build Default
        run: cargo build --features default
      - name: Cargo build Prod
        run: cargo build --features prod

#   Raspberry_Pi_3:
#     name: Raspberry Pi 3 Build
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#       - name: Install Rust
#         if: runner.os == 'Linux'
#         run: |
#           curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
#       - name: Cargo Build
#         run: cargo build --target=armv7-unknown-linux-gnueabihf
#       - name: Cargo Build Dev
#         run: cargo build --target=armv7-unknown-linux-gnueabihf --features dev
#       - name: Cargo Build Default
#         run: cargo build --target=armv7-unknown-linux-gnueabihf --features default
#       - name: Cargo build Prod
#         run: cargo build --target=armv7-unknown-linux-gnueabihf --features prod
